# Motrack

```v install https://github.com/Eliyaan/motrack```

Then go to `~/.vmodules/motrack` and you can run the examples if SDL>=3.0 is installed (check out https://github.com/vlang/sdl?tab=readme-ov-file#project-navigation)
